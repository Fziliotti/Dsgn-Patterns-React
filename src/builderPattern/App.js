import React from "react";

function App() {

  // TODO: A ideia aqui é fazer uma chamada a api FAKE, daí cosntruir o carro a partir do que veio da requisicao, fazendo a regra de negocio e modificando o que tem q modificar dentro desse builder -- regras de negocio dentro dele, e ele também dispara erro se algo vir inconsistente. (refletir se vale a pena criar uma classe separada que cuida da validacao dos parametros que vierem da api para nao sujar o builder, mas acho q seria mais complexidade e para projetos grandes)
  return (
    <div className="App">
    </div>
  );
}

export default App;
