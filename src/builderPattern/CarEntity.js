export default class Car {
  constructor(){
    this.name = name;
    this.brand = brand;
    this.km =km;
  }
}

// In the regular implementation, you will create a Car object with new Car('Gol', 'Fiat', 500). If pass 3months, probabily you will forget the sequence of parameters